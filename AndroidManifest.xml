<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.harshini.jbasuser" >

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.GHSNayanagr"
        android:name=".Jbas">

        <activity android:name=".SplashActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".authencation.ForgetPasswordActivity" />
        <activity android:name=".authencation.LoginActivity" />
        <activity android:name=".authencation.RegisterActivity" />
        <activity android:name=".developer.Developer" />
        <activity android:name=".FullImageView" />
        <activity
            android:name=".ebook.PdfViwerActivity"
            android:theme="@style/Custom" />
        <activity
            android:name=".ebook.EbookActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity" />


    </application>

</manifest>